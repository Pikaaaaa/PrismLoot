# --- Local (SQLite). Copy to .env for npm run dev ---
DATABASE_URL="file:./dev.db"

# --- Production (Vercel + Neon). SQLite files do not persist on Vercel. ---
# Use Neon's pooled URI (host contains -pooler). sslmode=require is required.
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require"
# Optional unpooled URI for Prisma db push. Falls back to DATABASE_URL with `-pooler` stripped.
# DIRECT_URL="postgresql://USER:PASSWORD@ep-xxxx.REGION.aws.neon.tech/neondb?sslmode=require"

# long random string — never commit the real value
ADMIN_SECRET=

# Unguessable URL slug for the operator console (do not use /admin)
ADMIN_PATH="/pl-console-9f3k"

# Public origin when hosted (no trailing slash). Required for Steam OpenID realm/return_to.
# On Vercel this must be the exact https://….vercel.app URL (or custom domain) and you must Redeploy after saving it.
NEXT_PUBLIC_SITE_URL="https://your-app.vercel.app"

# Optional. Fetches Steam persona name + avatar on first login (GetPlayerSummaries).
# Without it, the account still works with steamid64 and a fallback display name.
STEAM_API_KEY=

# Optional session HMAC. Falls back to ADMIN_SECRET if unset.
SESSION_SECRET=

# Cashier addresses (overlay the built-in placeholders)
# USDT TRC-20: set a real Tron T-address to enable live deposits + chain watch (no demo/placeholder text).
DEPOSIT_ADDR_USDT_TRC20="TEkD256tbTegJNinx4wwgfxFUtUBC4oUoi"
# Optional: set to 0 to disable live watch while keeping the address visible.
# DEPOSIT_LIVE_USDT_TRC20=0
# Optional TronGrid API key for higher rate limits
# TRONGRID_API_KEY=
DEPOSIT_ADDR_USDT_ERC20="0xDE00PRISMLOOTUSDTDEMO0000000000000001"
# Bitcoin: legacy (1/3…) or native SegWit (bc1…). Shown in cashier when valid (no demo/placeholder text).
DEPOSIT_ADDR_BTC="16wjJpb7UbHXmcoruy2m75LUvBUsr6vAhr"
# TRX on TRON (TRC20): Tron T-address. Shown in cashier when valid (no demo/placeholder text).
DEPOSIT_ADDR_TRX="TEkD256tbTegJNinx4wwgfxFUtUBC4oUoi"
DEPOSIT_ADDR_LTC="ltc1qprismlootdemodemo00000000000000"
DEPOSIT_ADDR_ETH="0x74510307b3317f41e8fd06596675c433725c38f7"
# Solana: base58 address. Shown in cashier when valid (no demo/placeholder text).
DEPOSIT_ADDR_SOL="sx7VGX8NorXJRMBCTnB7F34kcijBcaYF1EPeEfse44v"
DEPOSIT_ADDR_USDC_ERC20="0xDE00PRISMLOOTUSDCDEMO000000000000003"
DEPOSIT_ADDR_USDC_TRC20="TPRISMDEMOUSDCTRC20XXXXXXXXXXXXXXX"
# USDC BEP-20 on BSC: EVM address. Shown in cashier when valid (no demo/placeholder text).
DEPOSIT_ADDR_USDC_BEP20="0x74510307b3317f41e8fd06596675c433725c38f7"
# TON: friendly address (UQ… or EQ…). Shown in cashier when valid (no demo/placeholder text).
DEPOSIT_ADDR_TON="UQCLng8whrsbbJiQ1OrdYyjv1uRJm5Vt6avsaMGDzURRVtFj"
